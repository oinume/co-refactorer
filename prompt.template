あなたはプログラミング言語Goのエキスパートです。以下のdiffを参考にして、以下の {{ .TargetPaths }} を同じように書き換えて、中身を出力してほしいです。なお、出力はMarkdown形式で以下のようにしてください。

出力形式
### <file>
```
<content>
```


### diff
```
{{ .Diff }}
```


{{ range .TargetFiles }}
### {{ .Path }}
```
{{ .Content }}
```

{{ end }}
